#include <stdio.h>
#include <stlib.h>

int main(void)
{
    char move;
    int blocks;

    move = 'L';
    blocks = 5;

    navigate(move, blocks);
}

void navigate(char move, int blocks)
{
    switch (char)move {
        case 'N':
        
    }
}